/**
 * todo Elaborar um programa que apresente como resultado os quadrados dos números inteiros existentes na faixa de valores de 15 a 200.
 */

for (let i = 15; i <= 200; i++) {
  console.log(Math.pow(i, 2));
}
